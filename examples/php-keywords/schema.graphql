type Query {
    print: Parent
}

interface Parent {
    a: Int
}

type Switch implements Parent {
    a: Int
    for: abstract
}

type AnotherType implements Parent {
    a: Int
}

enum abstract {
    class
}

input new {
    unset: ID
}

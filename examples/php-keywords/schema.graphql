type Query {
    print: Abstract
    cases: [Case!]!
}

interface Abstract {
    int: Int
}

type Switch implements Abstract {
    for: abstract
    int: Int
}

enum abstract {
    class
}

input new {
    unset: Float
}

type Case {
    id: ID!
}

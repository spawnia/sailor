type Query {
    node(id: ID!): Node!
}

interface Node {
    id: ID!
}

type Post implements Node {
    id: ID!
    title: String
}

type Task implements Node {
    id: ID!
    done: Boolean!
}

type User implements Node {
    id: ID!
    name: String
}

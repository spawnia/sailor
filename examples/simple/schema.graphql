type Query {
    scalarWithArg(arg: String): ID
    twoArgs(first: String, second: Int): ID
    singleObject: SomeObject
    nonNullable: String!
}

type SomeObject {
    value: Int
    nested: SomeObject
}
